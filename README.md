# forge-minions-demo

A sandbox repository for [**Forge minions**](https://github.com/salehaiftikharr/agent-forge):
autonomous agents that pick up an issue, fix it, run the tests, and open a pull
request — only when the fix passes without breaking anything.

The pull requests on this repo were opened by a minion, not by a human.
